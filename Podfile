# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

pod 'Firebase', '>= 2.5.0'
pod 'FirebaseUI', '~> 0.4'
target 'sampleGroups' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sampleGroups
  
  target 'sampleGroupsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sampleGroupsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
